import Register from "@/app/components/Login/Register";
import React from "react";

export const metadata = {
  title: "4P - Register",
};

const page = () => {
  return (
    <>
      <Register />
    </>
  );
};

export default page;
